{
  "name": "skillgemplanner",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "electron-zip-packager ./ --arch=all --ignore=\".git(ignore|modules)|.idea\" --asar --out=../ --overwrite",
    "run": "electron ."
  },
  "author": "MedianAura",
  "license": "ISC",
  "dependencies": {
    "backbone": "^1.3.3",
    "backbone-relational": "^0.10.0",
    "bootstrap": "^3.3.7",
    "fs-extra": "^2.0.0",
    "glob": "^7.1.1",
    "jquery": "^3.1.1",
    "lodash": "^4.17.4",
    "nunjucks": "^3.0.0",
    "sortablejs": "^1.5.1",
    "sprintf": "^0.1.5"
  },
  "devDependencies": {
    "electron": "^1.6.1"
  }
}
